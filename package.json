{
  "name": "backend-engineer-test",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "run-docker": "docker-compose up -d --build",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:integration": "./spec/run-tests.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.8"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.38",
    "fastify": "^4.28.1",
    "pg": "^8.12.0"
  }
}